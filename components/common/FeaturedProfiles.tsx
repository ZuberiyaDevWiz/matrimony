import { FC } from 'react';

export interface FeaturedProfilesProps {
    img: string;
}

const FeaturedProfiles: FC = () => (
    <section>
        <h1>hello profile section</h1>
    </section>
);

export default FeaturedProfiles;
